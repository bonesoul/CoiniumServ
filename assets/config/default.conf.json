{
	# changing values in this file will no have no effects as it's actually loaded as an embedded assembly!
	# to overload values, rename settings-sample.conf.json as settings.conf.json and use it.

    # log configuration
	"logs": {
		"root": "logs",
		"targets": [
			{
				"enabled": true,
				"type": "console",
				"level": "verbose"
			},
			{
				"enabled": true,
				"type": "file",
				"filename": "server-{Date}.log",
				"rolling": true,
				"level":	"information"
			}
        ]	
	},

	# website configuration
	"website": {
		"enabled": false,
		"bind": "0.0.0.0",
		"port": 80
	},
    
    # database configuration.
	"database": {
		"redis": {
			"enabled": false,
			"host": "127.0.0.1",
			"port": 6379
		}
	}
}