{
	"enabled": false,
	
	"coin": "litecoin.json",

	# coin daemon config
	"daemon": {
		"host": "127.0.0.1",
		"port": 9333,
		"username": "user",
		"password": "password"
	},

	# stratum server config
	"stratum": {
		"enabled": true,
		"bind": "0.0.0.0",
		"port": 3333,
		"diff": 16
	},

	# vanilla server config
	"vanilla": {
		"enabled": true,
		"bind": "localhost",
		"port": 3334			
	},

	# payment processing
	"payments": {
		"enabled": false,
		"interval": 60,
		"minimum": 1
	},

	# mpos compat mode
	"mpos": {
		"enabled": false,
		"database": {
			"host": "127.0.0.1",
			"port": 3306,
			"name": "db-name",
			"user": "username",
			"password": "password"
		}
	}	
}